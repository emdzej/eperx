packages:
  - "packages/*"
  - "apps/*"

# vitest depends on esbuild, whose postinstall installs the platform binary.
# Without this pnpm skips it and vitest fails at startup.
onlyBuiltDependencies:
  - "esbuild"
patchedDependencies:
  sqlite-wasm-http@1.2.0: patches/sqlite-wasm-http@1.2.0.patch
